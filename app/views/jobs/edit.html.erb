<h1>Edit Job Details for: <%= @job.first_signer_last_name if @job.people.first %></h1>
<div class="row">
  <div class="col-lg-8">
    <%= render partial: 'form', locals: { user: @user, job: @job } %>
  </div>
</div>
